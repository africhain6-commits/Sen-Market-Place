kind = "mobile"
previewPath = "/sen-market-mobile/"
title = "SenMarket Mobile"
version = "1.0.0"
id = "artifacts/sen-market-mobile"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/sen-market-mobile/" ]
localPort = 18611

[services.development]
run = "pnpm --filter @workspace/sen-market-mobile run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/sen-market-mobile", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/sen-market-mobile", "run", "serve" ]

[services.env]
PORT = "18611"
BASE_PATH = "/sen-market-mobile/"
